const ErrorMessage = () => {
  return (
    <div>
      Sorry, something went wrong. We are working on getting this fixed as soon
      as we can. Try again later...
    </div>
  );
};

export default ErrorMessage;
